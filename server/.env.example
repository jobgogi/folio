# 서버 설정
PORT=3000
NODE_ENV=development

# JWT
JWT_SECRET=your-secret-here
JWT_EXPIRES_IN=1h

# Database (docker-compose 환경에서는 자동 주입됨)
DATABASE_URL=postgresql://user:password@localhost:5432/folio

# NAS
NAS_MOUNT_PATH=/mnt/nas
